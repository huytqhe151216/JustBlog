@using FA.JustBlock.Core.Models

@model List<Category>

@foreach (var category in Model)
{

    <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="@Url.Action("Index", "Post", new { category = category.Name })">@category.Name</a></li>
}